# sendgrid-type-gen

## Installation

```sh
npm i -g sendgrid-type-gen
```

## Useage

```sh
sendgrid-gen-type 
   --api-key {API_KEY}
   [ --template-id={templateId} ]
     [ --fname={fileName} ]
     [ --iname={interfaceName} ]
```
